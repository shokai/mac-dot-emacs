# .emacs for Mac

## Install emacs30

    % brew install emacs

## Setup

    % mkdir ~/.emacs.d
    % cd ~/.emacs.d
    % git init
    % git remote add origin git://github.com/shokai/mac-dot-emacs.git
    % git pull origin master
